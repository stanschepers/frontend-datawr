/*
This a list of all API URLS we use

Should always end with _URL and be in all capitals
*/

const PROFILE_URL = 'profile/'

let OBTAIN_TOKEN_URL = 'obtain_token/'

export default {
    PROFILE_URL, OBTAIN_TOKEN_URL
}